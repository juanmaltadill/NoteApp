<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.juanmaltadill.noteapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:longClickable="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteApp">
        <activity
            android:name=".SelectIconActivity"
            android:exported="false" />
        <activity
            android:name=".SelectDateAndHourActivity"
            android:exported="false" />
        <activity
            android:name=".CrearNotaActivity"
            android:exported="false" />
        <activity
            android:name=".NotesListActivity"
            android:exported="false" />
        <activity android:name=".NuevaNotaActivity" />
        <activity android:name=".NuevaListaActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>